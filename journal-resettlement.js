const scene=document.querySelector('.ap-scene');
const chapters=[...document.querySelectorAll('.ap-chapter')];
let active=-1;let queued=false;let supported=false;let selectedTime='morning';
const scenarios={morning:{title:'The morning plan',time:'10:00',period:'AM',items:[['Work','Before the 1 p.m. shift; visit length still matters.'],['Travel','The planned morning bus arrives at 9:40 a.m.'],['Caregiving','School pickup is at 3 p.m.; allow time for the return journey.']],timing:'Before the work shift',timingNote:'Allow for the visit and return trip.',travel:'A morning route',travelNote:'The example bus arrives at 9:40 a.m.'},afternoon:{title:'Same visit. A different set of arrangements.',time:'2:00',period:'PM',items:[['Work','The visit now overlaps with the 1 p.m. shift.'],['Travel','The morning route no longer fits. The example afternoon bus arrives at 1:55 p.m., leaving five minutes.'],['Caregiving','The visit is one hour before school pickup. Another arrangement may be needed.']],timing:'A work & pickup overlap',timingNote:'Work at 1 p.m. Pickup at 3 p.m.',travel:'Reconsider the route',travelNote:'Arrival at 1:55 leaves five minutes.'}};
function renderScene(){const showSupport=supported&&active===4;scene.classList.toggle('supported',showSupport);scene.classList.toggle('afternoon',selectedTime==='afternoon');const choice=scenarios[selectedTime];const clock=document.querySelector('.ticket-time');clock.replaceChildren(document.createTextNode(choice.time));const period=document.createElement('span');period.textContent=choice.period;clock.append(period);scene.querySelectorAll('[data-before]').forEach(el=>el.textContent=el.dataset[showSupport?'after':'before']);if(!showSupport&&active>=2){document.querySelector('.orbit-time strong').textContent=choice.timing;document.querySelector('.orbit-time>span:last-child').textContent=choice.timingNote;document.querySelector('.orbit-travel strong').textContent=choice.travel;document.querySelector('.orbit-travel>span:last-child').textContent=choice.travelNote;}}
function update(){queued=false;const mobile=window.innerWidth<=800;const target=mobile?Math.min(window.innerHeight-90,document.querySelector('.ap-stage').getBoundingClientRect().bottom+130):window.innerHeight*.5;let best=0;let distance=Infinity;chapters.forEach((chapter,i)=>{const box=chapter.getBoundingClientRect();const d=box.top>target?box.top-target:box.bottom<target?target-box.bottom:0;if(d<distance){distance=d;best=i;}});if(best===active)return;active=best;scene.dataset.step=String(best);chapters.forEach((c,i)=>c.classList.toggle('is-active',i===best));document.getElementById('ap-count').textContent=String(best+1).padStart(2,'0')+' / 05';document.getElementById('ap-stage-label').textContent=chapters[best].dataset.status;renderScene();}
function schedule(){if(!queued){queued=true;requestAnimationFrame(update);}}
window.addEventListener('scroll',schedule,{passive:true});window.addEventListener('resize',schedule);update();
const toggle=document.querySelector('.ap-support-toggle');toggle.addEventListener('click',()=>{supported=!supported;toggle.setAttribute('aria-pressed',String(supported));toggle.firstChild.textContent=supported?'View the practical questions ':'Add practical support ';toggle.querySelector('span').textContent=supported?'−':'+';renderScene();});
document.querySelectorAll('[data-time]').forEach(button=>button.addEventListener('click',()=>{selectedTime=button.dataset.time;document.querySelectorAll('[data-time]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));const choice=scenarios[selectedTime];document.getElementById('time-heading').textContent=choice.title;const list=document.getElementById('time-consequences');list.replaceChildren();choice.items.forEach(([label,text])=>{const li=document.createElement('li');const strong=document.createElement('strong');strong.textContent=label;const span=document.createElement('span');span.textContent=text;li.append(strong,span);list.append(li);});update();renderScene();}));
